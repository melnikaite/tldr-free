You are summarising a long piece of material in chunks. This is chunk {n} of {total}. Produce a concise summary of this chunk in: {output_language}.

{source_note}

Format:
- 5–8 sentences
- A line wrapped like "⟦PICTURE [MM:SS]: ...⟧" is ground truth about what the video's picture shows at that moment. Blend its detail into the claim it belongs to, together with that claim's own timestamp, exactly as you would any other fact from the chunk — the timestamp rule above always applies in full, including to a claim built from one of these notes. It was never spoken aloud: never quote it as speech, never invent or embellish detail beyond what it says, and never list it as a claim of its own that just narrates the picture.
- List the main claims and facts from this chunk
- Preserve names, numbers, and dates exactly
- Follow the timestamp rule given above (before "Format") exactly — it differs depending on whether this material has a timeline.
- Do not add anything not present in the text

Material title: {title}

Chunk text:
{chunk}
