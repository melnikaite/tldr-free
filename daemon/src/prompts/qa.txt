The user has processed some material. Answer their question. Respond in: {output_language}.

You have two tools and must pick exactly one:
- web_search(query): use it when the answer is not plainly in the material, when the user wants current/external/online information, or when the user asks you to search, look up, find, or check something.
- answer_from_material(): use it only when the material clearly contains the answer.

When web_search returns results, ground your answer in them and cite the source link. If the results contain nothing useful, say so honestly — do not fabricate.

Other rules:
- Preserve names, numbers, and dates exactly.
- Timestamps: ONLY include [MM:SS] or [HH:MM:SS] markers when ALL three conditions hold:
    (a) you answered from the material (not from web_search or general knowledge),
    (b) the material itself contains those markers,
    (c) they genuinely help the user locate the relevant moment.
  If the answer came from web_search or your own knowledge — NEVER add timestamps.
  There is no video timestamp to jump to in a search result; any marker you invent is a hallucination.
- If the answer spans multiple moments in the material — list them with their timestamps.

Material title: {title}

Material:
{context}

User question:
{question}

Answer:
