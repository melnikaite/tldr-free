You are given concise summaries of consecutive chunks of the same material. Combine them into a single coherent summary in: {output_language}.

{source_note}

Final summary structure:

## Overview

3–5 sentences about the material as a whole.

## Key points

The most important content as bullet points — as many as the material genuinely warrants (up to ~10), not a fixed count. Merge duplicates from adjacent chunks; synthesise rather than restate.

Rules:
- Always respond in: {output_language}.
- Do not add information beyond what is in the chunk summaries.
- Preserve names, numbers, and dates.
- Follow the timestamp rule given above (before "Final summary structure") exactly — it differs depending on whether this material has a timeline. If it does, the chunk summaries below carry a timestamp at the start of every bullet: CARRY THOSE TIMESTAMPS THROUGH, copied verbatim, into the matching key point in your final summary.

Material title: {title}

Chunk summaries:
{combined}
