You are looking at a few frames sampled, about one per second, from a short moment in a video. The speaker said, at this point: "{phrase}".

There is no question to answer here — you are writing a note for whoever writes this video's summary, so they can describe what the picture actually shows instead of guessing from the words alone.

Look at the frames and describe, briefly and concretely, what they show:
1. What is on screen — the setting, and any object(s), product(s), or person(s) visible.
2. Is there any readable text, a product label, a price, an on-screen interface, or burned-in writing? If so, transcribe it exactly as it appears.
3. Are hands or a person visibly performing an action across the frames? If so, describe concretely what is being done, to what.

Name a thing only as specifically as the picture itself supports: "a flat object on the desk", not "a tablet"; "something in her hand", not "a stylus" — unless you can genuinely tell which it is. This matters MORE here than it would for an ordinary question, precisely because there is no question anchoring your answer against something already known to be true — you are the only source of ground truth about the picture, so an invented detail here has nothing to correct it.

Do not guess, infer, or fill in what would be "plausible" given the topic the speaker is discussing. A vague, blurry, or ambiguous frame is not evidence of anything specific — do not describe it as if it were, and do not let the speaker's words talk you into seeing something that isn't clearly there.

Judging whether this is worth passing on is a SEPARATE step, reported only in the `relevant` field below — do not write that judgment, or any sentence about it, into your description. A generic shot of the speaker talking, with nothing being shown, adds nothing a transcript doesn't already have — that case is not worth it. A shown product, a demonstrated action, on-screen text, a chart, a diagram, a place, a specific person — anything the transcript's words alone could not tell a reader — is worth it.

Be concise: a few sentences, not a full paragraph. Write your answer in: {output_language}.

The frames above are given to you in order, numbered 1 to however many you were sent — frame 1 is the earliest.

Report your answer by calling the tool with these fields:
- finding: a plain, concrete description of what is on screen (points 1-3 above) — nothing else. Do NOT include any judgment of whether this is worth passing on, whether it "adds information the transcript lacks", or any other commentary about its own relevance — that verdict belongs ONLY in the `relevant` field below, never restated or justified in this text. The same grounding rules from above still apply here, in full — in particular: name a thing only as specifically as the picture supports, and never invent detail beyond what the frames actually show.
- relevant: true ONLY if the picture adds something the transcript alone does not — a shown object, a demonstrated action, readable on-screen text, a chart, a diagram, a specific place or person. false for a generic shot with nothing being shown (most commonly: just the speaker talking to camera) — that case must not be treated as a finding worth surfacing.
- best_frame_index: the number, from the frame numbering above, of the single frame to SHOW A PERSON as the evidence for what you described. This frame gets displayed on its own, so judge it on two things, in this order. First, it has to actually show the thing — the object, the readable text, the moment of the action. Second, among the frames that do, pick the one that is well captured: reject a frame where someone is caught mid-blink or with eyes shut, the head is turned away, the mouth hangs open mid-word, the body is in an awkward halfway pose, motion has smeared the picture, or the thing being shown is cut off by the edge of the frame. If every frame fails that second test, still pick the one that shows the thing best — a badly caught frame that shows it beats a handsome frame that doesn't. Give your best guess even when relevant is false.
